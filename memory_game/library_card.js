"use strict";

// Object to work with a single card when clicked
const card = {

    // Method to check if card that has been clicked is blank or has already been revealed

    // Method to accept a parameter representing the first card clicked and checks if the id attribute
    // tag equals to the id attribute of the current card

};